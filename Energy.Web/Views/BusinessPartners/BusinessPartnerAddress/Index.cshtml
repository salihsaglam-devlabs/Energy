@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["BusinessPartners.BusinessPartnerAddress.Title"].Value;
    ViewData["ScreenId"] = "BusinessPartners.BusinessPartnerAddress";
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div><h2>@T["BusinessPartners.BusinessPartnerAddress.Title"]</h2></div>
    </header>
    <div id="business-partner-address-grid"></div>
</section>

@section Scripts {
    <script src="~/js/business-partners/business-partner-address/business-partner-address.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(string.Format(T["Help.IntroEntity"].Value, ViewData["Title"]))),
            related: [
                @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["BusinessPartners.BusinessPartner.Title"].Value))
            ]
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.BusinessPartnersBusinessPartnerAddress.init("/business-partners/business-partner-addresses", "business-partner-address-grid", "BusinessPartners");
        });
    </script>
}
