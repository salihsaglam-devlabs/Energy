@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["BusinessPartners.BusinessPartnerContact.Title"].Value;
    ViewData["ScreenId"] = "BusinessPartners.BusinessPartnerContact";
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div><h2>@T["BusinessPartners.BusinessPartnerContact.Title"]</h2></div>
    </header>
    <div id="business-partner-contact-grid"></div>
</section>

@section Scripts {
    <script src="~/js/business-partners/business-partner-contact/business-partner-contact.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(string.Format(T["Help.IntroEntity"].Value, ViewData["Title"]))),
            related: [
                @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["BusinessPartners.BusinessPartner.Title"].Value))
            ]
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.BusinessPartnersBusinessPartnerContact.init("/business-partners/business-partner-contacts", "business-partner-contact-grid", "BusinessPartners");
        });
    </script>
}
