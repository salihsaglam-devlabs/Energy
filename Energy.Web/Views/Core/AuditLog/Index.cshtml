@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["Core.AuditLog.Title"].Value;
    ViewData["ScreenId"] = "Core.AuditLog";
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div><h2>@T["Core.AuditLog.Title"]</h2></div>
    </header>
    <div id="audit-log-grid"></div>
</section>

@section Scripts {
    <script src="~/js/core/audit-log/audit-log.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(string.Format(T["Help.IntroEntity"].Value, ViewData["Title"])))
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.CoreAuditLog.init("/core/audit-logs", "audit-log-grid", "Core");
        });
    </script>
}
