@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["Core.LocalizationResource.Title"].Value;
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div><h2>@T["Core.LocalizationResource.Title"]</h2></div>
    </header>
    <div id="localization-resource-grid"></div>
</section>

@section Scripts {
    <script src="~/js/core/localization-resource/localization-resource.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(string.Format(T["Help.IntroEntity"].Value, ViewData["Title"])))
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.CoreLocalizationResource.init("/core/localization-resources", "localization-resource-grid", "Core");
        });
    </script>
}
