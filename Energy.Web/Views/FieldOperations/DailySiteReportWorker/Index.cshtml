@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["FieldOperations.DailySiteReportWorker.Title"].Value;
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div><h2>@T["FieldOperations.DailySiteReportWorker.Title"]</h2></div>
    </header>
    <div id="daily-site-report-worker-grid"></div>
</section>

@section Scripts {
    <script src="~/js/field-operations/daily-site-report-worker/daily-site-report-worker.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(string.Format(T["Help.IntroEntity"].Value, ViewData["Title"]))),
            related: [
                @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["FieldOperations.DailySiteReport.Title"].Value)),
                @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["Organization.Employee.Title"].Value))
            ]
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.FieldOperationsDailySiteReportWorker.init("/field-operations/daily-site-report-workers", "daily-site-report-worker-grid", "FieldOperations");
        });
    </script>
}
