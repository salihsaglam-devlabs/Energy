@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["Operations.WorkOrderStatusHistory.Title"].Value;
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div><h2>@T["Operations.WorkOrderStatusHistory.Title"]</h2></div>
    </header>
    <div id="work-order-status-history-grid"></div>
</section>

@section Scripts {
    <script src="~/js/operations/work-order-status-history/work-order-status-history.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(string.Format(T["Help.IntroEntity"].Value, ViewData["Title"]))),
            related: [
                @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["Operations.WorkOrder.Title"].Value))
            ]
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.OperationsWorkOrderStatusHistory.init("/operations/work-order-status-histories", "work-order-status-history-grid", "Operations");
        });
    </script>
}
