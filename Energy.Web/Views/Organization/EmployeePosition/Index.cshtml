@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["Organization.EmployeePosition.Title"].Value;
    ViewData["ScreenId"] = "Organization.EmployeePosition";
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div><h2>@T["Organization.EmployeePosition.Title"]</h2></div>
    </header>
    <div id="employee-position-grid"></div>
</section>

@section Scripts {
    <script src="~/js/organization/employee-position/employee-position.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(string.Format(T["Help.IntroEntity"].Value, ViewData["Title"])))
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.OrganizationEmployeePosition.init("/organization/employee-positions", "employee-position-grid", "Organization");
        });
    </script>
}
