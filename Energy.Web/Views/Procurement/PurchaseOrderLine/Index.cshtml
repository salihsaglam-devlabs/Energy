@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["Procurement.PurchaseOrderLine.Title"].Value;
    ViewData["ScreenId"] = "Procurement.PurchaseOrderLine";
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div><h2>@T["Procurement.PurchaseOrderLine.Title"]</h2></div>
    </header>
    <div id="purchase-order-line-grid"></div>
</section>

@section Scripts {
    <script src="~/js/procurement/purchase-order-line/purchase-order-line.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["Procurement.PurchaseOrderLine.Help.Intro"].Value)),
            related: [
                @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["Core.Currency.Title"].Value)),
                @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["Catalog.Material.Title"].Value)),
                @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["Procurement.PurchaseOrder.Title"].Value)),
                @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["Requests.RequestLine.Title"].Value))
            ]
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.ProcurementPurchaseOrderLine.init("/procurement/purchase-order-lines", "purchase-order-line-grid", "Procurement");
        });
    </script>
}
