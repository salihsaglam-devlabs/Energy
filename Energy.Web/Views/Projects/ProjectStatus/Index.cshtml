@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["Projects.ProjectStatus.Title"].Value;
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div><h2>@T["Projects.ProjectStatus.Title"]</h2></div>
    </header>
    <div id="project-status-grid"></div>
</section>

@section Scripts {
    <script src="~/js/projects/project-status/project-status.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(string.Format(T["Help.IntroEntity"].Value, ViewData["Title"])))
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.ProjectsProjectStatus.init("/projects/project-statuses", "project-status-grid", "Projects");
        });
    </script>
}
