@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T[LocalizationKeys.RolesScreen.Title].Value;
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div>
            <h2>@T[LocalizationKeys.RolesScreen.Title]</h2>
            <p>@T[LocalizationKeys.RolesScreen.Subtitle]</p>
        </div>
    </header>

    <div id="roles-grid"></div>
</section>

@section Scripts {
    <script src="~/js/iam/roles.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["Help.IntroGeneric"].Value))
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.Roles.init();
        });
    </script>
}

