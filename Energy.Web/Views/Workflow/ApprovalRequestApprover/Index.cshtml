@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["Workflow.ApprovalRequestApprover.Title"].Value;
    ViewData["ScreenId"] = "Workflow.ApprovalRequestApprover";
}

<section class="energy-screen">
    <header class="energy-screen__header">
        <div><h2>@T["Workflow.ApprovalRequestApprover.Title"]</h2></div>
    </header>
    <div id="approval-request-approver-grid"></div>
</section>

@section Scripts {
    <script src="~/js/workflow/approval-request-approver/approval-request-approver.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(string.Format(T["Help.IntroEntity"].Value, ViewData["Title"]))),
            related: [
                @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["Workflow.ApprovalRequestStep.Title"].Value))
            ]
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppPages.WorkflowApprovalRequestApprover.init("/workflow/approval-request-approvers", "approval-request-approver-grid", "Workflow");
        });
    </script>
}
