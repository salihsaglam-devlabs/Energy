@using Energy.Localization
@inject Microsoft.AspNetCore.Mvc.Localization.IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["Workflow.Processes.Approval.Title"].Value;
}

<link rel="stylesheet" href="~/css/workflow/processes/approval/approval.css" asp-append-version="true" />

<section class="energy-screen energy-process">
    <header class="energy-screen__header">
        <div><h2>@T["Workflow.Processes.Approval.Title"]</h2></div>
    </header>
    <p class="energy-process__hint">@T["Workflow.Processes.Approval.Hint"]</p>
    <div id="approval-inbox-grid"></div>
</section>

@section Scripts {
    <script src="~/js/workflow/processes/approval/approval.index.js" asp-append-version="true"></script>
    <script>
        window.AppPageHelp = {
            intro: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(T["Workflow.Processes.Approval.Hint"].Value))
        };
        document.addEventListener("DOMContentLoaded", function () {
            window.AppProcesses.WorkflowApproval.init("/workflow/processes/approval", "approval-inbox-grid", {
                approve: "@T["Workflow.Processes.Approval.Actions.Approve"]",
                reject: "@T["Workflow.Processes.Approval.Actions.Reject"]",
                cancel: "@T["Workflow.Processes.Approval.Actions.Cancel"]",
                notePrompt: "@T["Workflow.Processes.Approval.NotePrompt"]"
            });
        });
    </script>
}

